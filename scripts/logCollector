#!/bin/bash
set -e

sensor="imu";
executeScript="scripts/mpuData.py";

cd ~/Engineering/Python/VSCode/sensorData || exit 1
pwd; ls -la;
mkdir -p logs;

LOG=logs/"${sensor}"_$(date +%Y%m%d_%H%M%S).csv;

mpremote connect /dev/ttyACM0 run "${executeScript}" | tee -a "$LOG"